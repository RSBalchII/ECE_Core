"""
Deprecated helper script.

This file previously contained an ad-hoc script that rewrote
`engine/tests/unit/native-module-manager.test.ts`. That behavior has
been intentionally removed to avoid keeping one-off local tooling in
the main codebase. If similar functionality is needed in the future,
it should live in a dedicated tooling directory with documentation.
"""
